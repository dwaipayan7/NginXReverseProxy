{
  "name": "nginx-reverse-proxy",
  "version": "1.0.0",
  "description": "A TypeScript-based project for managing Nginx reverse proxy configurations.",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index --config config.yaml",
    "build": "tsc -p .",
    "dev": "tsc-watch --onSuccess \"npm run start\""
  },
  "keywords": ["nginx", "reverse-proxy", "typescript", "yaml", "zod"],
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^20.8.2",
    "commander": "^13.1.0",
    "nodemon": "^3.1.9",
    "tsc-watch": "^6.2.1",
    "yaml": "^2.7.0",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
